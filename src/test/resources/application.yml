spring:
  datasource:
    url: jdbc:h2:mem:hhplus;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.show_sql: true
      hibernate.format_sql: true
  testcontainers:
    enabled: false
